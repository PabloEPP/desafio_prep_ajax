<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<%if@post.image.attached? %>
  <p>
    <strong>Image:</strong>
    <br><%= image_tag @post.image.variant(resize:"100x100")%>
  </p>
<%end%>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
